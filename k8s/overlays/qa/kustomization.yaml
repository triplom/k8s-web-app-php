apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../../base
- ingress.yaml

namespace: k8s-web-app-php-qa

patches:
- target:
    kind: Deployment
    name: k8s-web-app-php
  patch: |-
    - op: replace
      path: /spec/template/spec/imagePullSecrets
      value:
      - name: github-registry-secret

configMapGenerator:
- name: k8s-web-app-php-config
  behavior: merge
  literals:
  - ENVIRONMENT=qa
  - LOG_LEVEL=DEBUG